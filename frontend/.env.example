# Leave unset (or delete this line) to run on built-in demo data with no backend.
# Point at John's FastAPI server to go live. The backend now serves the full
# live surface on one URL: invoices (real Xero invoices merged ahead of seed
# data), /analytics/summary, the fix-queue actions, and the PDF check.
# VITE_API_URL=http://localhost:8000
